drop table if exists user CASCADE;
drop table if exists diary CASCADE ;

create table user (
    user_pk bigint generated by default as identity,
    user_role_type varchar(255),
    user_age bigint,
    user_id varchar(255),
    user_message varchar(255),
    user_nick_name varchar(20),
    user_password varchar(255),
    user_money bigint,
    primary key (user_pk)
);

create table diary (
   diary_pk bigint generated by default as identity,
   diary_content clob,
   diary_delete_date date,
   diary_date date,
   user_fk bigint,
   primary key (diary_pk)
);

INSERT INTO USER (USER_ROLE_TYPE, USER_AGE, USER_ID, USER_MESSAGE, USER_NICK_NAME, USER_PASSWORD, USER_MONEY) VALUES ('ADMIN',24,'sleeper','관리자다','관리자','sleeper123@',0);
INSERT INTO DIARY (diary_content, diary_date, user_fk) VALUES ('행복한 하루 되세요.','2022-12-25',1);